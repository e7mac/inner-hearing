export const getPublicUrl = (filename) => {
    return `../inner-hearing/${filename}`;
};

export const Loader = () => {
    return <div className="flex justify-center items-center h-screen">Wait a moment...</div>;
};
